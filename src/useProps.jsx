function useProps({ob}) {
    return (
        <div>
            <h1> Uses of Props in React JS </h1>
            <h2> Name: {ob.name} </h2>
            <h2> Age: {ob.age} </h2>
        </div>
    )
}
export default useProps;